# 小林的工具箱 · 使用文档

跨平台系统优化集成应用，基于 Tauri 2.0 + React + TypeScript 构建，采用 iOS 26 液态玻璃设计语言。

---

## 一、环境说明

小林的工具箱有两种运行模式，功能可用性取决于运行环境：

| 运行模式 | 启动方式 | 功能范围 |
|---------|---------|---------|
| 浏览器预览 | `npm run dev` | 仅数据展示 + 隐私清理（浏览器 API） |
| 桌面应用 | `tauri dev` / `tauri build` | 全部功能（真实系统调用） |

> 桌面应用模式下，所有优化模块通过 Rust 命令真实读写系统；浏览器预览模式下，优化模块会提示「需桌面环境」。

---

## 二、功能清单与状态

### 图例

- ✅ 完全可用（真实执行）
- 🖥️ 需桌面环境（Tauri 模式下可用）
- 📋 仅查看（扫描可执行，操作开发中）
- 🔧 开发中

### 通用模块（两端共用）

| 模块 | 扫描 | 执行 | 状态 | 说明 |
|------|------|------|------|------|
| 一键加速 | ✅ | 🖥️ | 🖥️ | 结束非必要后台进程释放内存（保护系统进程） |
| 隐私清理 | ✅ | ✅ | ✅ | 浏览器端清理 caches/localStorage/sessionStorage/IndexedDB/Cookies |
| 网络优化 | 🖥️ | 🖥️ | 🖥️ | DNS 优选 / 测速 / 网络重置（DNS 缓存 + Winsock + TCP/IP） |
| 安全扫描 | 🖥️ | 🖥️ | 🖥️ | 可疑启动项 / 下载目录恶意文件 / Defender / 防火墙状态检测 |

### 电脑专属模块

| 模块 | 扫描 | 执行 | 状态 | 说明 |
|------|------|------|------|------|
| 垃圾清理 | 🖥️ | 🖥️ | 🖥️ | 扫描临时目录/缓存/回收站，勾选删除释放空间 |
| 注册表清理 | - | - | 🔧 | 无效键值/残留项清理（开发中） |
| 启动项管理 | 🖥️ | 📋 | 📋 | 读取注册表 + 启动文件夹，禁用功能开发中 |
| 磁盘优化 | 🖥️ | 📋 | 📋 | 各磁盘占用分析，碎片整理开发中 |
| 驱动管理 | - | - | 🔧 | 驱动检测/备份/回滚（开发中） |
| 软件管理 | 🖥️ | 📋 | 📋 | 读取已安装软件列表，卸载功能开发中 |
| 系统信息 | ✅ | - | ✅ | 硬件配置/系统版本/运行时长（首页展示） |
| 小林 AI 助手 | ✅ | ✅ | ✅ | 智能命令执行（70+ 功能 / 13 分类，侧边栏钛金橙强调入口） |

### 移动专属模块（桌面端等价实现）

移动模块在桌面环境映射为等价的实用功能，需 Tauri 桌面应用运行。

| 模块 | 扫描 | 执行 | 说明 |
|------|------|------|------|
| 应用清理 | 🖥️ | 🖥️ | 扫描 Chrome/Edge/Firefox 缓存、系统 Temp、缩略图、WER 错误报告等 10 个缓存目录 |
| 电池优化 | 🖥️ | 🖥️ | 通过 wmic 获取进程内存占用排行 Top 10，标记高耗电进程 |
| 流量监控 | 🖥️ | 🖥️ | 通过 netstat -ano 统计每个进程的网络连接数，tasklist 映射进程名 |
| 自启管理 | 🖥️ | 🖥️ | 读取注册表 Run 启动项，通过 reg 命令备份后删除（可恢复） |
| 应用锁 | 🖥️ | 🖥️ | XOR 流加密文件，生成 .xiaolin_enc 加密文件，密码派生密钥 |
| 骚扰拦截 | 🖥️ | 🖥️ | 读写 hosts 文件，批量添加/移除域名屏蔽（广告/追踪/恶意） |
| 权限管理 | 🖥️ | 🖥️ | 通过 netsh advfirewall 管理防火墙规则，添加/移除程序网络访问阻止 |
| 文件清理 | 🖥️ | 🖥️ | 扫描下载/文档/桌面/视频/图片目录中的大文件/重复文件/安装包/日志/缓存 |

### 数据展示模块（首页）

| 模块 | 状态 | 说明 |
|------|------|------|
| 状态总览 | ✅ | CPU/内存/磁盘/网络 实时圆环 |
| 硬件信息 | ✅ | 10 项硬件详情（系统/处理器/显卡/内存/主板/磁盘/电池/网络/屏幕/浏览器） |
| 监控中心 | ✅ | 实时进程列表 + CPU/内存占用 |

### OPPO Find X8s 专属模块（ADB 桥接）

针对 OPPO Find X8s（天玑 9400 / 哈苏影像 / 100W SUPERVOOC / 120Hz）定制的专属功能，通过 ADB 桥接读写手机系统，需桌面应用模式 + ADB 环境。

| 模块 | 状态 | 说明 |
|------|------|------|
| 设备检测 | 🖥️ | ADB 检测 + Find X8s 识别（PJZ110/CPH2681）+ USB 调试引导 |
| 设备信息 | 🖥️ | getprop 读取型号/Android/ColorOS + 天玑9400/G925/哈苏/100W 固定规格 |
| 性能调度 | 🖥️ | 8 核 CPU 频率 + GPU 频率 + 温度传感器 Top8 + 均衡/性能/省电/超级省电切换 |
| 哈苏影像 | 🖥️ | DCIM 扫描 · RAW 文件清理 · 相机设置备份 · 启用哈苏专业模式 |
| 快充电池 | 🖥️ | dumpsys battery 解析 + SUPERVOOC 快充检测 + 6000mAh 设计容量 + 智能充电保护 |
| ColorOS 优化 | 🖥️ | 10 个缓存目录扫描清理 + 8 个预装应用禁用/恢复（pm disable-user） |
| 120Hz 屏幕 | 🖥️ | 自适应/60Hz/120Hz 切换 + 护眼模式 + 暗色模式 + 亮度调节滑块 |

---

## 三、运行方式

### 1. 浏览器预览（开发调试）

```bash
npm install
npm run dev
```

访问 `http://localhost:5174/`

### 2. 桌面应用（完整功能）

```bash
# 开发模式
npm run tauri dev

# 打包发布
npm run tauri build
```

打包产物位于 `src-tauri/target/release/`。

---

## 四、优化模块使用步骤

所有优化模块遵循统一流程：**扫描 → 报告 → 勾选 → 执行**。

### 垃圾清理

1. 在首页点击「垃圾清理」模块卡片
2. 进入详情页，点击「开始扫描」
3. 系统扫描临时目录、浏览器缓存、回收站（深度 3 层，仅记录 >100KB 文件）
4. 扫描完成，查看可清理项列表（按大小降序，最多 200 条）
5. 勾选要删除的文件（默认全选）
6. 点击「执行优化」删除文件并显示释放空间

### 隐私清理

1. 点击「隐私清理」模块卡片
2. 点击「开始扫描」列出可清理项
3. 勾选要清理的项（Cache Storage / localStorage / sessionStorage / IndexedDB / Cookies）
4. 点击「执行优化」真实清理浏览器数据

> 浏览器预览模式下也可真实执行隐私清理。

### 启动项管理

1. 点击「启动项管理」模块卡片
2. 点击「开始扫描」读取注册表 HKCU/HKLM Run 键 + 启动文件夹
3. 查看启动项列表（名称/命令/位置）
4. 禁用功能开发中，当前仅支持查看

### 软件管理

1. 点击「软件管理」模块卡片
2. 点击「开始扫描」读取注册表 Uninstall 键
3. 查看已安装软件列表（名称/版本/发布者/大小）
4. 卸载功能开发中，当前仅支持查看

### 磁盘优化

1. 点击「磁盘优化」模块卡片
2. 点击「开始扫描」读取各磁盘占用
3. 查看磁盘列表（名称/类型/已用/可用/百分比）
4. 碎片整理开发中，当前仅支持查看占用

### 一键加速

1. 点击「一键加速」模块卡片
2. 查看加速说明
3. 桌面环境下可结束后台进程释放内存（系统关键进程受保护，禁止结束）

### 网络优化

通过 Rust 命令真实执行 DNS 优选、测速与网络重置，需桌面应用模式运行。

**使用步骤：**

1. 点击「网络优化」模块卡片
2. 点击「开始扫描」启动 DNS 测速
3. 系统将：
   - 读取当前 DNS（`ipconfig /all`）
   - 对 5 个主流 DNS 节点 ping 测试：114 DNS、阿里 DNS、腾讯 DNS、Google DNS、Cloudflare
   - 按延迟升序排列，标记最快节点
4. 报告页面显示：
   - 当前 DNS 地址
   - 各 DNS 节点延迟（ms）与状态（优秀/正常/较慢/不可达）
   - 网络重置选项
5. 勾选要设置的 DNS 节点（默认选中延迟最低的），或勾选「重置网络」选项
6. 点击「执行优化」：
   - 设置 DNS：通过 `netsh interface ip set dns` 写入系统（需管理员权限）
   - 重置网络：依次执行 `ipconfig /flushdns` + `netsh winsock reset` + `netsh int ip reset`（重启后生效）

**Rust 命令：** `scan_network` / `set_dns` / `reset_network`

### 安全扫描

通过多层检测识别系统中的潜在威胁与安全配置问题，需桌面应用模式运行。

**使用步骤：**

1. 点击「安全扫描」模块卡片
2. 点击「开始扫描」启动深度扫描
3. 系统将扫描以下项目：
   - **可疑启动项**：检测注册表 Run 键与启动文件夹中指向 temp 目录、`$recycle.bin` 或脚本扩展名（.bat/.cmd/.vbs/.js/.ps1）的项
   - **下载目录恶意文件**：扫描 `~/Downloads` 中的可执行文件，识别：
     - 双扩展名文件（如 `report.pdf.exe`，高危）
     - 伪装系统进程（如 `svchost.exe`、`explorer.exe` 在非系统目录，严重）
   - **临时目录可疑文件**：扫描 `~/AppData/Local/Temp` 中 >50KB 的 .exe/.dll 文件
   - **Windows Defender 状态**：通过 PowerShell `Get-MpComputerStatus` 查询实时保护是否启用
   - **防火墙状态**：通过 `netsh advfirewall show allprofiles state` 检查所有配置文件
   - **系统更新时间**：通过 `Get-HotFix` 读取最近一次补丁安装时间
4. 报告页面显示扫描摘要 + 威胁列表（含严重等级图标 + 角标）：
   - 🔴 CRITICAL（严重）：伪装系统进程 / Defender 禁用
   - 🟠 HIGH（高危）：双扩展名文件 / 防火墙关闭 / 可疑启动项
   - 🟡 MEDIUM（中危）：临时目录可执行文件
5. 勾选要处理的威胁项（默认全选），点击「执行优化」：
   - 文件类威胁：移动到隔离目录 `%LOCALAPPDATA%/xiaolin-toolbox/quarantine/`（不直接删除，可恢复）
   - Defender 禁用：执行 `Set-MpPreference -DisableRealtimeMonitoring $false`（需管理员权限）
   - 防火墙关闭：执行 `netsh advfirewall set allprofiles state on`（需管理员权限）

**Rust 命令：** `scan_security` / `quarantine_threat` / `enable_defender` / `enable_firewall`

> **注意：** DNS 设置、网络重置、Defender/防火墙启用操作均需管理员权限运行桌面应用，否则会返回失败提示。

### 移动专属模块（桌面端等价实现）

移动模块在桌面环境映射为等价的实用功能，所有模块均通过 Rust 命令真实执行。

#### 应用清理

扫描浏览器与应用缓存目录，清理无用缓存文件。

**使用步骤：**
1. 点击「应用清理」模块卡片
2. 点击「开始扫描」
3. 系统扫描 10 个缓存目录：Chrome 缓存、Edge 缓存、Firefox 缓存、系统 Temp、Windows 缩略图、图标缓存、最近文档、INetCache、WER 错误报告、CrashDumps
4. 报告显示各目录大小，按降序排列
5. 勾选要清理的目录，点击「执行清理」
6. 系统删除目录内容（保留目录本身），统计释放空间

**Rust 命令：** `scan_app_cache` / `clean_app_cache`

#### 电池优化

扫描高 CPU/内存占用进程，标记高耗电项。

**使用步骤：**
1. 点击「电池优化」模块卡片
2. 点击「开始扫描」
3. 系统通过 `wmic process get Name,WorkingSetSize` 获取进程内存占用
4. 报告显示 Top 10 进程的内存占比（>50MB 才显示）
5. 勾选高耗电进程，点击「执行优化」
6. 系统标记进程名，建议通过任务管理器结束（避免误杀系统进程）

**Rust 命令：** `scan_power_processes`

#### 流量监控

统计每个进程的网络连接数，识别网络活动频繁的进程。

**使用步骤：**
1. 点击「流量监控」模块卡片
2. 点击「开始扫描」
3. 系统通过 `netstat -ano` 获取所有 ESTABLISHED/LISTENING 连接
4. 通过 `tasklist /fo csv` 将 PID 映射到进程名
5. 报告显示 Top 10 进程的连接数和占比
6. 勾选高连接进程，点击「执行优化」
7. 系统提示可通过「权限管理」添加防火墙规则阻止

**Rust 命令：** `scan_network_connections`

#### 自启管理

读取注册表启动项，禁用不必要的自启程序。

**使用步骤：**
1. 点击「自启管理」模块卡片
2. 点击「开始扫描」
3. 系统读取注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 启动项
4. 报告显示所有自启程序名称和命令路径
5. 勾选要禁用的启动项，点击「执行优化」
6. 系统通过 `reg query` 读取原值 → `reg add` 备份到 `StartupBackup` → `reg delete` 删除原值（可恢复）

**Rust 命令：** `list_startup` / `disable_startup`

#### 应用锁

使用 XOR 流加密保护敏感文件。

**使用步骤：**
1. 点击「应用锁」模块卡片
2. 点击「开始扫描」
3. 系统扫描文档/下载/桌面目录中的 `.xiaolin_enc` 加密文件
4. 报告显示已加密文件列表
5. 要加密新文件：在 AI 助手中输入「加密文件 D:\secret.txt」（需提供文件路径）
6. 系统弹出密码输入框，输入密码后执行 XOR 加密
7. 原文件被替换为 `.xiaolin_enc` 加密文件
8. 解密：在 AI 助手中输入「解密文件 D:\secret.txt.xiaolin_enc」并输入密码

**Rust 命令：** `scan_locked_files` / `encrypt_file` / `decrypt_file`

> **注意：** XOR 加密为轻量级保护，不适用于高敏感数据。生产环境建议改用 AES-256。

#### 骚扰拦截

通过修改 hosts 文件屏蔽广告/追踪/恶意域名。

**使用步骤：**
1. 点击「骚扰拦截」模块卡片
2. 点击「开始扫描」
3. 系统读取 `C:\Windows\System32\drivers\etc\hosts` 文件
4. 报告显示已屏蔽的域名列表，自动分类（广告/追踪/恶意/其他）
5. 要添加新屏蔽：在 AI 助手中输入「屏蔽域名 doubleclick.net」
6. 勾选要移除的域名，点击「执行优化」可批量移除屏蔽

**Rust 命令：** `scan_hosts` / `add_hosts_block` / `remove_hosts_block`

> **注意：** 修改 hosts 文件需管理员权限。

#### 权限管理

通过防火墙规则控制程序的网络访问权限。

**使用步骤：**
1. 点击「权限管理」模块卡片
2. 点击「开始扫描」
3. 系统通过 `netsh advfirewall firewall show rule name=all` 获取所有阻止规则
4. 报告显示 action=Block 的规则列表
5. 要阻止新程序：在 AI 助手中输入「阻止网络 D:\suspicious.exe」
6. 系统通过 `netsh advfirewall firewall add rule` 添加出站阻止规则
7. 勾选要恢复的程序，点击「执行优化」可移除阻止规则

**Rust 命令：** `scan_firewall_rules` / `add_firewall_block` / `remove_firewall_block`

> **注意：** 添加/删除防火墙规则需管理员权限。

#### 文件清理

扫描大文件、重复文件、安装包残留、日志、缓存文件。

**使用步骤：**
1. 点击「文件清理」模块卡片
2. 点击「开始扫描」
3. 系统扫描 5 个目录（下载/文档/桌面/视频/图片），深度 2-3 层
4. 分类识别：
   - **安装包**：.exe / .msi / .apk 文件
   - **日志**：.log 文件
   - **缓存**：.tmp / .bak / .old 文件
   - **大文件**：>100MB 的任意文件
   - **重复文件**：同名文件出现在多个目录
5. 报告显示文件列表（含路径、大小、分类），按大小降序
6. 勾选要删除的文件，点击「执行清理」
7. 系统批量删除选中文件，统计删除数量

**Rust 命令：** `scan_large_files` / `delete_files`

### OPPO Find X8s 专属工作室

通过 ADB 桥接为 OPPO Find X8s 定制的专属功能模块，需桌面应用模式 + ADB 环境。

**入口位置：** 左侧导航栏「Find X8s 工作室」翡翠绿入口（呼吸光晕 + Smartphone 图标）

**前置准备：**

1. 安装 ADB（Android Platform Tools）：https://developer.android.com/tools/releases/platform-tools
2. 手机开启开发者选项：设置 → 关于手机 → 版本信息 → 连续点击版本号 7 次
3. 开启 USB 调试：设置 → 系统 → 开发者选项 → USB 调试
4. 用数据线连接手机与电脑，手机弹窗允许 USB 调试

**使用步骤：**

1. 点击侧边栏「Find X8s 工作室」进入页面
2. 系统自动检测 ADB 可用性 + 设备连接 + Find X8s 识别（型号 PJZ110/CPH2681）
3. 连接成功后，顶部显示绿色「Find X8s 已连接」徽章
4. 通过 Tab 切换 6 大功能模块：

#### 设备信息

- 读取 getprop 设备标识（型号/品牌/Android/ColorOS/安全补丁/Bootloader）
- 展示 Find X8s 硬件规格（天玑 9400 / Immortalis-G925 / 哈苏三摄 / 100W SUPERVOOC）
- 屏幕分辨率、密度、当前电量与电池温度

#### 性能调度（天玑 9400）

- 读取 8 核 CPU 实时频率（scaling_cur_freq）+ GPU 频率（kgsl-3d0/gpuclk）
- 读取温度传感器 Top 8（thermal_zone，按温度降序，>60°C 标红）
- 一键切换 4 种模式：均衡 / 性能 / 省电 / 超级省电（通过 settings put oppo.performance.mode）

#### 哈苏影像

- 扫描 DCIM/Camera 目录，统计照片/视频与 RAW(.dng) 文件数量
- 启用哈苏专业模式（am broadcast com.oppo.camera.ENABLE_HASSELBRAD_MODE）
- 备份相机设置到 /sdcard/OPPO/Camera/backup/
- 清理 RAW 文件（find + rm，二次确认后执行）

#### 快充电池

- 解析 dumpsys battery：电量/温度/电压/健康度/状态/电池技术
- 检测 SUPERVOOC 100W 快充状态（vooc_charging_enable）
- 读取充电循环次数与充电计数器（需 root 权限）
- 显示 6000mAh 设计容量与智能充电保护状态

#### ColorOS 优化

- 扫描 10 个 ColorOS 缓存目录（相册/SystemUI/天气/浏览器/软件商店/云服务/视频/联系人/日志）
- 统计可释放空间，一键清理缓存（rm -rf）
- 管理预装应用：禁用/恢复 8 个 OPPO 预装应用（游戏空间/软件商店/云服务/视频/音乐/书签/指南针/健康检查）
- 通过 `pm disable-user --user 0` 禁用，`pm enable` 恢复

#### 120Hz 屏幕

- 读取当前刷新率/亮度/护眼/暗色/色彩模式
- 切换刷新率：自适应(120Hz) / 标准(60Hz) / 高(120Hz)
- 一键切换护眼模式与暗色模式
- 亮度滑块调节（0-255，松手时写入 settings put screen_brightness）

**Rust 命令：** `oppo_check_adb` / `oppo_device_info` / `oppo_performance_mode` / `oppo_camera_backup` / `oppo_battery_health` / `oppo_coloros_clean` / `oppo_screen_control`

> **注意：**
> - 所有功能需 Tauri 桌面环境 + ADB 已安装 + 手机已开启 USB 调试并连接
> - CPU 频率、温度传感器、充电循环次数等 sysfs 读取可能需要 root 权限
> - 预装应用禁用通过 `pm disable-user` 实现（无需 root，可恢复）
> - 清理 RAW 文件操作不可恢复，会弹出二次确认
> - ADB 路径检测顺序：ADB 环境变量 → ANDROID_HOME/platform-tools → 常见安装位置 → PATH

### 小林 AI 助手

集成在「命令行工具」模块中的智能命令助手，支持 **70+ 种实用功能**，覆盖 **13 个分类**。

**入口位置：**

- **桌面端**：左侧导航栏顶部「小林 AI 助手」钛金橙强调入口（呼吸光晕 + 流光效果，醒目易找）
- **移动端**：首页顶部栏 Sparkles 图标按钮（primary + shimmer 样式）
- **首页**：「本机工具」分区中的「小林 AI 助手」模块卡片

**使用方式：**

1. 通过上述任一入口进入 AI 助手页面
2. 三栏布局：左侧分类面板 + 中部命令列表 + 右侧聊天流
3. 顶部搜索框可跨分类搜索命令（按标题/关键词/描述匹配）
4. 点击分类标签切换功能类别（标签显示命令计数）
5. 点击命令按钮快速执行，或在底部输入框直接输入命令/自然语言
6. 桌面端可点击 PanelLeft 图标折叠/展开左侧面板；移动端为浮层覆盖，执行命令后自动关闭

**支持的功能分类：**

| 分类 | 命令数 | 功能 |
|------|--------|------|
| 系统控制 | 8 | 关机、重启、休眠、锁屏、注销、取消关机、清空回收站、刷新 DNS |
| 快捷启动 | 15 | 计算器、记事本、资源管理器、任务管理器、注册表、CMD、PowerShell、画图、控制面板、设备管理器、截图工具、时钟、屏幕键盘、放大镜、字符映射表 |
| 网络工具 | 7 | 本机 IP、Ping 测试、DNS 查询、URL 编码/解码、网速测试、端口查询、路由追踪 |
| 编码转换 | 8 | Base64、SHA-256 哈希、UUID 生成、时间戳转换、进制转换、HTML 转义、Unicode、JWT 解析 |
| 文本处理 | 10 | 字数统计、大小写转换、JSON 格式化、文本反转、去重、排序、替换、去除空白、拼音首字母、正则测试 |
| 时间日期 | 6 | 当前时间、今日日期、星期、倒计时、本月日历、年度进度 |
| 数学计算 | 6 | 四则运算、开方、幂运算、三角函数、随机数、统计计算 |
| 单位换算 | 6 | 长度、重量、温度、面积、速度、数据存储 |
| 颜色工具 | 4 | 随机颜色、RGB/HEX 互转、颜色信息（HSL/HSV/CMYK）、渐变生成 |
| 开发工具 | 6 | Git 状态、Node 版本、NPM 包列表、Lorem Ipsum 占位文本、Cron 解析、HTTP 状态码 |
| 文件操作 | 4 | 打开路径、列出目录、创建文件夹、文件信息 |
| 剪贴板 | 3 | 复制文本、读取剪贴板、清空剪贴板 |
| 信息查询 | 5 | 系统信息、电池状态、屏幕信息、网络状态、帮助 |

**示例命令：**

```
关机
重启
锁屏
打开计算器
打开记事本
ping baidu.com
本机 IP
base64 编码 hello world
生成 UUID
当前时间
今天是星期几
倒计时 2026-12-31
json 格式化 {"a":1,"b":2}
字数统计 这是一段测试文本
计算 1+2*3
开方 144
幂 2 10
sin 30
随机数 1 100
统计 1,2,3,4,5
长度 1 米 英尺
温度 100 摄氏 华氏
随机颜色
rgb 转 hex 255 128 0
颜色信息 #ff8000
渐变色
git 状态
node 版本
lorem 3
cron */5 * * * *
http 404
打开路径 D:\
列目录 C:\Users
创建文件夹 D:\test
复制文本 你好
读取剪贴板
拼音 你好世界
正则 ^\d+$ 测试123
帮助
```

> **注意：**
> - 系统控制、快捷启动、文件操作类功能需在桌面应用模式下使用（通过 Rust `run_shell` 命令执行，内置黑名单拦截 format/del/rd/rmdir/mkfs/dd 等危险命令）
> - 编码转换、文本处理、时间日期、数学计算、单位换算、颜色工具、剪贴板等功能在浏览器预览模式下也可正常执行
> - 数学计算仅允许数字和 + - * / ( ) 运算符，禁用其他字符防注入
> - 危险操作（关机/重启/清空回收站等）会弹出二次确认对话框

---

## 五、液态玻璃效果说明

应用全面采用 iOS 26 液态玻璃设计语言：

- **glass-tile**：基础玻璃材质（半透明 + 模糊 + 边框 + 内阴影）
- **glass-tile-strong**：钛金橙强调变体（用于激活态/重要元素）
- **glass-tile-edge**：顶部高光边（模拟材质厚度）
- **glass-tile-hover**：跟随鼠标的圆形光斑
- **glass-track**：进度条轨道
- **glass-fill**：进度条填充（含 shimmer 流光）
- **glass-row**：列表行 hover 渐变
- **glass-divider**：玻璃分隔线
- **glass-shine**：静态斜向高光（已去掉左右晃动动画）

---

## 六、技术栈

| 层 | 技术 |
|----|------|
| 框架 | Tauri 2.0 |
| 前端 | React 18 + TypeScript + Vite |
| 样式 | Tailwind CSS + 自定义玻璃工具类 |
| 动画 | Framer Motion |
| 图标 | lucide-react |
| 后端 | Rust + sysinfo + walkdir + dirs |
| 路由 | react-router-dom |

---

## 七、安全保护

- **系统进程保护**：一键加速功能内置 14 个关键系统进程白名单（system/svchost/explorer/dwm/lsass 等），禁止结束
- **命令黑名单**：AI 助手的 `run_shell` 命令内置黑名单，拦截 format/del/rd/rmdir/mkfs/dd 等危险命令
- **表达式校验**：数学计算仅允许数字和 + - * / ( ) 运算符，禁用其他字符防止代码注入
- **危险操作确认**：关机/重启/清空回收站等危险操作会弹出 `window.confirm` 二次确认对话框
- **威胁隔离不删除**：安全扫描发现的恶意文件移动到隔离目录（`%LOCALAPPDATA%/xiaolin-toolbox/quarantine/`），不直接删除以便恢复
- **管理员权限提示**：DNS 设置、网络重置、Defender/防火墙启用等需管理员的操作失败时会明确提示
- **只读扫描**：所有扫描操作仅读取不修改，删除/隔离操作需用户勾选确认
- **浏览器降级**：非桌面环境下优化模块明确提示，不执行危险操作
