feat(v1.1.7): 修复多轮对话上下文 + 安全策略增强 + 硬件信息修正

- 修复多轮对话上下文丢失：历史消息包含 AI 回复
- getUserIdleSeconds 统一返回 Infinity，invoke 失败不阻塞
- GetTickCount64 替换 GetTickCount，避免 49.5 天溢出
- run_shell 黑名单与前端一致（20个），词边界正则，检查 args 防绕过
- kill_process 保护进程补全到 14 个
- dailyCostLimit 实际拦截，任务开始前检查今日费用
- 视觉模型失败 UI 提示 + 图片附件 5张/10MB 限制
- 更新下载进度回调（修正 DownloadEvent 字段映射）
- 版本号动态读取，不再硬编码
- WaitingCard 显示工具名和恢复状态
- write_file 相对路径重定向 + PowerShell 转义 + 扩展名推断
- get_desktop_path 优先 PowerShell（OneDrive 兼容）
- hardware_info cpu_cores 估算 + battery.level 字段
- waitForUserIdle 10分钟超时 + 监听器清理
- 黑名单预编译正则
- screenshot_region 边界验证
- search_files 深度限制 3 层
- power_restart/shutdown 用 status() 等待
