# 小林的工具箱 · 打包与使用说明

本文档详细说明如何将「小林的工具箱」打包为 Windows 安装包，以及安装后的使用方法。

---

## 一、打包环境准备

打包 Tauri 应用需要以下三样工具。**首次打包前必须全部安装。**

### 1. Node.js（已安装）

项目已使用 Node.js 进行前端开发，确保版本 ≥ 18。

```powershell
node --version   # 应输出 v18.x 或更高
```

### 2. Rust 工具链（⚠️ 当前未安装，必须安装）

Tauri 的后端用 Rust 编写，打包时需要 Rust 编译器。

**安装步骤：**

1. 访问 https://rustup.rs/
2. 下载 `rustup-init.exe`
3. 双击运行，按提示选择默认安装（选项 `1`）
4. 安装完成后**重启终端**，验证：

```powershell
rustc --version   # 应输出 rustc 1.77.x 或更高
cargo --version   # 应输出 cargo 1.77.x 或更高
```

> Rust 安装会自动配置环境变量，无需手动设置。

### 3. Visual Studio C++ 构建工具（⚠️ 需确认已安装）

Rust 在 Windows 上编译需要 MSVC 链接器。如果你已安装 Visual Studio，可能已包含。

**安装步骤：**

1. 访问 https://visualstudio.microsoft.com/visual-cpp-build-tools/
2. 下载「生成工具」安装包
3. 在安装器中勾选 **「使用 C++ 的桌面开发」** 工作负载
4. 点击安装（约 6GB 空间）

> 如果不安装此项，打包时会报错 `error: linker 'link.exe' not found`。

### 4. WiX Toolset（Tauri 自动下载）

生成 MSI 安装包需要 WiX。Tauri 在首次打包时会**自动下载**，无需手动安装。

---

## 二、打包步骤

### 1. 生成应用图标（✅ 已完成）

项目已生成全平台图标（Windows/macOS/iOS/Android），位于 `src-tauri/icons/`。

如需更换图标：

```powershell
# 1. 准备一张 1024×1024 的 PNG 图片（命名为 app-icon.png）
# 2. 运行图标生成命令
npx tauri icon app-icon.png
```

这会自动生成所有平台所需尺寸的图标文件。

### 2. 执行打包

打开终端，进入项目根目录：

```powershell
# 方式一：使用 npm 脚本（推荐）
npm run tauri:build

# 方式二：直接使用 npx
npx tauri build
```

打包过程（首次约 10-20 分钟）：

1. `npm run build` — 编译前端（TypeScript + Vite）
2. `cargo build --release` — 编译 Rust 后端（首次需下载依赖，较慢）
3. 生成安装包 — WiX 生成 MSI，NSIS 生成 EXE

> 首次打包会下载大量 Rust 依赖（约 500MB），后续打包会快很多。

### 3. 打包产物

打包完成后，安装包位于：

```
src-tauri/target/release/
├── 小林的工具箱.exe              # 免安装版（可直接运行）
├── bundle/
│   ├── msi/
│   │   └── 小林的工具箱_1.0.0_x64_zh-CN.msi    # MSI 安装包
│   └── nsis/
│       └── 小林的工具箱_1.0.0_x64-setup.exe     # NSIS 安装包
```

| 产物 | 说明 | 推荐场景 |
|------|------|----------|
| MSI 安装包 | Windows 官方安装格式，支持组策略部署 | 企业/正式分发 |
| NSIS EXE | 轻量安装程序，启动快 | 个人使用（推荐） |
| 独立 EXE | 免安装直接运行 | 便携使用 |

### 4. 只生成特定格式（可选）

如果不需要两种安装包，可以指定目标：

```powershell
# 只生成 NSIS EXE
npx tauri build --bundles nsis

# 只生成 MSI
npx tauri build --bundles msi
```

---

## 三、安装与卸载

### 安装

1. 双击安装包（`.msi` 或 `-setup.exe`）
2. 按安装向导提示点击「下一步」
3. 选择安装目录（默认 `C:\Program Files\小林的工具箱`）
4. 点击「安装」
5. 安装完成，可选择立即启动

### 卸载

- **方式一**：设置 → 应用 → 找到「小林的工具箱」→ 卸载
- **方式二**：安装目录下运行 `uninstall.exe`

---

## 四、使用指南

### 启动应用

- 桌面快捷方式：双击「小林的工具箱」图标
- 开始菜单：搜索「小林的工具箱」

### 界面布局

```
┌──────────────────────────────────────────────┐
│  [Find X8s 工作室]  │                        │
│  [小林 AI 助手]     │     主内容区            │
│  ─────────────────  │  （首页/优化/AI/OPPO）  │
│  📊 首页            │                        │
│  🛠️ 本机工具        │                        │
│  📱 跨设备工具       │                        │
│  ─────────────────  │                        │
│  设备硬件信息卡片    │                        │
├──────────────────────────────────────────────┤
│  状态栏：CPU · 内存 · 磁盘 · 网络 · 运行时长    │
└──────────────────────────────────────────────┘
```

### 核心功能快速上手

#### 1. 首页（状态总览）

启动后默认进入首页，展示：
- **状态圆环**：CPU / 内存 / 磁盘 / 网络实时占用
- **硬件信息**：10 项硬件详情（处理器/显卡/内存/主板/磁盘等）
- **模块入口**：本机工具 + 跨设备工具卡片

#### 2. 一键加速

1. 点击「一键加速」卡片
2. 查看加速说明
3. 点击执行 → 自动结束非必要后台进程（14 个系统关键进程受保护）

#### 3. 垃圾清理

1. 点击「垃圾清理」卡片
2. 点击「开始扫描」
3. 等待扫描完成（临时目录/缓存/回收站，深度 3 层）
4. 勾选要删除的文件（默认全选）
5. 点击「执行优化」→ 显示释放空间

#### 4. 小林 AI 助手

**入口**：侧边栏钛金橙入口（呼吸光晕）

- **13 个分类**：系统控制 / 快捷启动 / 网络工具 / 编码转换 / 文本处理 / 时间日期 / 数学计算 / 单位换算 / 颜色工具 / 开发工具 / 文件操作 / 剪贴板 / 信息查询
- **70+ 命令**：点击命令按钮或直接输入
- **搜索**：顶部搜索框跨分类搜索

**示例命令：**
```
关机              → 60 秒后关机（弹出确认）
打开计算器         → 启动 Windows 计算器
ping baidu.com   → 网络连通性测试
base64 编码 你好   → 返回 5L2g5aW9
计算 1+2*3        → 返回 7
随机颜色           → 生成随机 HEX 色值
```

#### 5. 网络优化

1. 点击「网络优化」卡片
2. 点击「开始扫描」→ 自动测速 5 个 DNS 节点
3. 查看各节点延迟，选择最快的
4. 点击「执行优化」→ 设置系统 DNS（需管理员权限）

#### 6. 安全扫描

1. 点击「安全扫描」卡片
2. 点击「开始扫描」→ 检测可疑启动项/恶意文件/Defender/防火墙
3. 查看威胁列表（🔴严重 / 🟠高危 / 🟡中危）
4. 勾选威胁项，点击「执行优化」→ 隔离到安全目录

### OPPO Find X8s 专属工作室

**入口**：侧边栏翡翠绿入口（Smartphone 图标 + 呼吸光晕）

通过 ADB 桥接为你的 OPPO Find X8s 定制专属功能。

#### 连接准备

1. **安装 ADB**
   - 下载：https://developer.android.com/tools/releases/platform-tools
   - 解压到 `C:\platform-tools\`
   - 将 `C:\platform-tools` 添加到系统环境变量 PATH

2. **手机开启 USB 调试**
   - 设置 → 关于手机 → 版本信息 → 连续点击「版本号」7 次（开启开发者模式）
   - 设置 → 系统 → 开发者选项 → 开启「USB 调试」

3. **连接手机**
   - 用数据线连接手机与电脑
   - 手机弹窗「是否允许 USB 调试」→ 点击允许

4. **验证连接**
   - 打开「Find X8s 工作室」页面
   - 点击「重新检测」
   - 显示绿色「Find X8s 已连接」徽章即成功

#### 6 大功能模块

| 模块 | 功能 |
|------|------|
| **设备信息** | 读取型号/系统版本/硬件规格（天玑9400/G925/哈苏/100W） |
| **性能调度** | 8 核 CPU 频率监控 + 4 种模式切换（均衡/性能/省电/超级省电） |
| **哈苏影像** | DCIM 扫描 · RAW 清理 · 相机设置备份 · 启用哈苏专业模式 |
| **快充电池** | 电量/温度/电压/健康度 · SUPERVOOC 100W 快充检测 · 循环次数 |
| **ColorOS** | 10 个缓存目录清理 · 8 个预装应用禁用/恢复 |
| **120Hz 屏幕** | 刷新率切换(自适应/60/120) · 护眼模式 · 暗色模式 · 亮度调节 |

---

## 五、以管理员权限运行

以下功能需要管理员权限，请右键「小林的工具箱」→「以管理员身份运行」：

| 功能 | 原因 |
|------|------|
| 网络优化 - 设置 DNS | 修改系统网络配置 |
| 网络优化 - 重置网络 | 执行 ipconfig/netsh 系统命令 |
| 安全扫描 - 启用 Defender | 修改 Windows Defender 设置 |
| 安全扫描 - 启用防火墙 | 修改防火墙状态 |
| 骚扰拦截 | 修改 hosts 文件 |
| 权限管理 | 添加/删除防火墙规则 |

> 非管理员模式下这些功能会返回失败提示，但不影响其他功能使用。

---

## 六、常见问题

### Q: 打包时报错 `error: linker 'link.exe' not found`

**A:** 未安装 Visual Studio C++ 构建工具。参考「环境准备」第 3 步安装。

### Q: 打包时报错 `error: failed to run rustc`

**A:** Rust 未正确安装。运行 `rustup default stable` 设置默认工具链。

### Q: 安装包体积太大

**A:** 修改 `src-tauri/Cargo.toml` 的 release profile 已优化（LTO + strip + opt-level=s），最终体积约 8-15MB。

### Q: 应用启动后白屏

**A:** 检查 `src-tauri/tauri.conf.json` 中的 `frontendDist` 路径是否正确指向 `../dist`。确保 `npm run build` 先成功执行。

### Q: OPPO Find X8s 连接失败

**A:** 检查以下几点：
1. ADB 是否安装且在 PATH 中（终端运行 `adb version` 验证）
2. 手机是否开启 USB 调试
3. 数据线是否支持数据传输（非纯充电线）
4. 手机上是否点击了「允许 USB 调试」弹窗
5. 尝试执行 `adb kill-server` 后重新连接

### Q: 某些功能提示「需桌面环境」

**A:** 这些功能需要通过 Tauri 桌面应用运行，不能在浏览器中使用。请使用安装包安装后运行，而非 `npm run dev`。

### Q: 如何更新应用

**A:** 当前版本需手动下载新安装包覆盖安装。应用数据（设置/隔离文件）不会丢失。

---

## 七、项目配置信息

| 项目 | 值 |
|------|-----|
| 应用名称 | 小林的工具箱 |
| 版本 | 1.0.0 |
| 标识符 | com.xiaolin.toolbox |
| 发布者 | XiaoLin Studio |
| 技术栈 | Tauri 2.0 + React 18 + TypeScript + Rust |
| 设计语言 | iOS 26 液态玻璃 |
| 安装包格式 | MSI (WiX) + EXE (NSIS) |
| 安装语言 | 简体中文 |

---

## 八、技术支持

- 详细功能清单见项目根目录 `USAGE.md`
- 源代码位于 `src/`（前端）和 `src-tauri/`（后端）
- 图标源文件：`app-icon.png`（1024×1024）
- 图标生成脚本：`gen-icon.ps1`
